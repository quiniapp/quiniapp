import { Router } from 'express';
export declare class ScheduleRouter {
  router: Router;
  private controller;
  constructor();
  private setupRoutes;
  private newSchedulehandler;
  private getAllScheduleHandler;
  private updateScheduleHandler;
  private deleteScheduleHandler;
}
